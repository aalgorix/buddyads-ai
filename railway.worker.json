{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm exec prisma generate --schema=prisma/schema.prisma"
  },
  "deploy": {
    "startCommand": "pnpm exec prisma db push --schema=prisma/schema.prisma && pnpm --filter @buddyads/worker start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
